#include <iostream>
#include <cmath>
using namespace std;

int main() {
    
    int num;
    cout<<"enter the number "<<endl;
    cin>>num;

    cout<<"the square root of "<<num<<" is "<<sqrt(num);
}